declare var $ENV: BaseUrl;

interface BaseUrl {
  ENVIRONMENT: string;
  API_GATEWAY: String;
  MS_AUTH: string;
  MS_USER: string;
  MS_QUESTIONS: string;
  MS_ANSWERS: string;
  MS_COMMENTS: string;
}